node_modules

# Build output (generated locally, not for the repo)
apps/web/dist/

# Environment files (contain secrets). Only .env.example is tracked.
.env
.env.*
!.env.example

# PocketBase local data and binaries (per-environment, large, sometimes sensitive)
apps/pocketbase/pb_data/
apps/pocketbase/pb_data_backup/
apps/pocketbase/pb_snapshots/
apps/pocketbase/pocketbase
apps/pocketbase/pocketbase.exe
apps/pocketbase/pocketbase_linux
apps/pocketbase/pocketbase_win.zip
apps/pocketbase/pb38.zip
